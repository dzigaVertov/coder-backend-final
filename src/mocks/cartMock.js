import {faker} from '@faker-js/faker';

export function crearMockCarts(cantidad=1){
    let carts =[];

    for (let i=0; i<cantidad; i++){
        carts.push({
            productos : 
        })
    }
}
